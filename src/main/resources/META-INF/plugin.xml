<!-- Plugin Configuration File: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN, cannot be changed between the plugin versions. -->
    <id>com.purringlabs.gitworktree.git-worktree-manager</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>Git Worktree Manager</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="purringlabs@gmail.com" url="https://github.com/MingLu0">Purring Labs</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        Simplify Git worktree management in Android Studio and IntelliJ IDEA.<br>
        <br>
        Create, list, and delete Git worktrees with just a few clicks. Perfect for developers who need to work on multiple features simultaneously without the overhead of switching branches or maintaining multiple clones.<br>
        <br>
        <b>Features:</b><br>
        <ul>
            <li>Create new worktrees (new branch or existing local/remote branch)</li>
            <li>View all worktrees in a convenient tool window</li>
            <li>Optionally copy selected ignored files into the new worktree</li>
            <li>Optionally copy Claude Code project context into the new worktree</li>
            <li>Merge, pull, push, and prune worktrees</li>
            <li>Copy worktree path/branch/commit to clipboard</li>
            <li>Open worktree directories in the IDE Terminal (requires the Terminal plugin)</li>
            <li>Reveal worktree directories in the OS file manager</li>
            <li>Delete worktrees with one click</li>
            <li>Automatically open worktrees in new IDE windows</li>
        </ul>
    ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.compose</depends>
    <depends optional="true" config-file="kotlin.xml">org.jetbrains.kotlin</depends>
    <depends>Git4Idea</depends>
    <depends optional="true" config-file="terminal.xml">org.jetbrains.plugins.terminal</depends>
    <resource-bundle>messages.MyMessageBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="GitWorktrees"
                    displayName="Git Worktrees"
                    factoryClass="com.purringlabs.gitworktree.gitworktreemanager.MyToolWindowFactory"
                    anchor="bottom"
                    icon="/icons/toolWindowIcon.svg"/>
    </extensions>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK2="true"/>
    </extensions>
</idea-plugin>
