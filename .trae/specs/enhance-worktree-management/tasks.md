# 任务列表 (Tasks)
- [x] 任务 1：添加对 `prune` 的 Git 命令支持
  - [x] 子任务 1.1：在 `GitWorktreeService` 中添加 `pruneWorktrees` 方法以执行 `git worktree prune` 命令。
  - [x] 子任务 1.2：在 `WorktreeRepositoryContract` 中定义 `pruneWorktrees` 并在 `WorktreeRepository` 中实现它。
- [x] 任务 2：为新操作更新视图模型 (ViewModel)
  - [x] 子任务 2.1：在 `WorktreeState` 中添加 `isPruning` (清理中) 和 `pullingBranch` (拉取中) 的状态变量。
  - [x] 子任务 2.2：在 `WorktreeViewModel` 中添加 `pruneWorktrees` 方法，更新状态、调用存储库方法并刷新列表。
  - [x] 子任务 2.3：在 `WorktreeViewModel` 中添加 `pullBranch` 方法，调用现有的 `pullBranch` 服务并更新状态。
- [x] 任务 3：增强工具窗口 UI (MyToolWindow.kt)
  - [x] 子任务 3.1：在 `WorktreeListContent` 的操作栏中，"Refresh" 按钮旁添加 "Prune" 按钮。
  - [x] 子任务 3.2：扩展 `WorktreeItem` 的上下文菜单，增加 "Pull from remote"、"Open in Terminal" 和 "Reveal in Explorer" 选项。
  - [x] 子任务 3.3：实现打开终端的逻辑（使用 IDE 内置终端窗口的 API）。
  - [x] 子任务 3.4：实现打开系统资源管理器的逻辑（使用 `RevealFileAction` 或 `BrowserUtil` API）。
  - [x] 子任务 3.5：将所有 UI 回调连接到相应的新操作逻辑。
