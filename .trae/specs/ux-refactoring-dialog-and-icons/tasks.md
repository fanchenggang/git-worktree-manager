# 任务列表 (Tasks)
- [x] 任务 1：设计并实现 `CreateWorktreeDialog`
  - [x] 子任务 1.1：创建 `CreateWorktreeDialog.kt` 并继承自 IntelliJ 的 `DialogWrapper`。
  - [x] 子任务 1.2：在对话框内构建 `JPanel` 表单，包含工作树名称输入框、分支名称输入框（可随名称联动）以及“Copy ignored files”复选框。
  - [x] 子任务 1.3：暴露获取用户输入数据的方法（如 `getWorktreeName()`, `getBranchName()`, `shouldCopyIgnoredFiles()`）。
- [x] 任务 2：重构“创建工作树”的触发逻辑 (MyToolWindow.kt)
  - [x] 子任务 2.1：移除 `WorktreeListContent` 中的 `onRequestWorktreeName`, `onRequestBranchName`, `onRequestCopyIgnoredFiles` 参数。
  - [x] 子任务 2.2：在点击“Create Worktree”按钮时，实例化并显示 `CreateWorktreeDialog`。
  - [x] 子任务 2.3：当对话框返回 `OK` 后，执行原有的名称校验、分支校验逻辑，并根据复选框状态调用 `onCreateWorktreeWithIgnoredFiles` 或 `onCreateWorktree`。
- [x] 任务 3：优化 `WorktreeItem` 悬浮交互
  - [x] 子任务 3.1：在 `WorktreeItem` 中，当 `isHovered` 为 true 时，移除原有的文字 "Delete" 按钮。
  - [x] 子任务 3.2：引入 AllIcons 中的标准图标（或者使用简单的文本/Unicode 图标代替如果找不到合适的资源，或者使用插件内置资源），创建悬浮的图标按钮行（Terminal, Folder, Trash）。
  - [x] 子任务 3.3：为这些图标按钮绑定相应的回调（`onOpenInTerminal`, `onRevealInExplorer`, `onDelete`），并处理删除按钮被禁用时的提示逻辑。